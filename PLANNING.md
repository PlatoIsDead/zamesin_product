# PLANNING.md — AJTBD RAG Bot

## 2026-06-08 — Переход на Telegram-бота

### Контекст
Бот переезжает на production-сервер (Timeweb Cloud, `147.45.137.205`). Streamlit больше не нужен.

### Решения

| Решение | Обоснование |
|---------|-------------|
| Убрать Streamlit полностью | Только Telegram-бот, нет смысла держать лишнюю зависимость |
| `build_index.py` переведён на OpenAI embeddings | sentence-transformers не был в `requirements.txt` — архитектурное несоответствие; теперь один источник правды |
| Эмбеддинги собираются локально и коммитятся | Сервер стартует без пересборки, не тратится API на каждый деплой |
| `embeddings_cache.npy` в `.gitignore` | Бинарный файл ~5 MB; решение: копировать через `scp` отдельно или убрать из `.gitignore` перед деплоем |

### Открытые вопросы / следующие шаги

- [ ] Деплой на `root@147.45.137.205` (инструкция в TASK.md)
- [ ] Решить: коммитить `embeddings_cache.npy` или копировать через scp — сейчас он в `.gitignore`
- [ ] Добавить `.gitignore` исключение для `data/chunks_cache.json` если он тоже большой
